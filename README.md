# 📮 Анализатор почтовых марок

Python-скрипт для анализа изображений почтовых марок с использованием OpenAI GPT-4.1 multimodal API. Извлекает визуальную информацию и дополняет её справочными данными из открытых источников.

---

## 📋 Описание

Этот проект позволяет анализировать изображения почтовых марок через OpenAI GPT-4.1, извлекая визуально доступную информацию (страна, номинал, год, сюжет и т.д.) и дополняя её справочным описанием на основе общедоступных знаний.

### Основные возможности

* ✅ Анализ изображений почтовых марок через OpenAI GPT-4.1
* ✅ Извлечение визуальной информации (страна, номинал, год, текст, цвета)
* ✅ Справочная информация о марке и историческом контексте
* ✅ Сохранение результатов в формате JSON
* ✅ Генерация голосового описания марки (опционально)
* ✅ Детерминированные результаты (temperature=0.1)
* ✅ Чёткое разделение визуальных данных и справочной информации

---

## 🚀 Быстрый старт

### Предварительные требования

* Python 3.10+
* OpenAI API Key (получить на [platform.openai.com](https://platform.openai.com))
* Модель GPT-4.1 с поддержкой vision (например, `gpt-4o` или `gpt-4-turbo`)

### Установка

1. **Клонируйте репозиторий:**

```bash
git clone <repository_url>
cd pem02_stamps
```

2. **Создайте виртуальное окружение:**

```bash
python3 -m venv venv
```

3. **Активируйте виртуальное окружение:**

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

4. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

5. **Создайте файл `.env` в корне проекта:**

```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1
OPENAI_TTS_MODEL=tts-1
OPENAI_TTS_VOICE=alloy
OPENAI_TTS_FORMAT=mp3
```

6. **Запустите анализ:**

```bash
python analyze_stamp.py input/stamp1.jpg
```

Или с генерацией голосового описания:

```bash
python analyze_stamp.py input/stamp1.jpg --tts
```

---

## 🛠 Технологии

* **Python 3.10+** — основной язык
* **OpenAI API** — работа с GPT-4.1 multimodal моделями
* **python-dotenv** — управление переменными окружения

---

## 📁 Структура проекта

```
pem02_stamps/
├── input/              # Папка для входных изображений
├── output/             # Папка для результатов (JSON, аудио, тексты)
├── analyze_stamp.py    # Основной скрипт анализа
├── json_to_voice.py    # Модуль генерации голосового описания
├── requirements.txt    # Зависимости проекта
├── .env               # Переменные окружения (не в git)
├── .gitignore         # Исключения для git
└── README.md          # Документация
```

---

## 🎯 Использование

### Запуск анализа

После настройки окружения и добавления изображения в папку `input/`, запустите скрипт:

```bash
python analyze_stamp.py input/название_изображения.jpg
```

### Формат входных данных

Поддерживаемые форматы изображений:
* `.jpg` / `.jpeg`
* `.png`
* `.gif`
* `.webp`

### Формат выходных данных

Результат сохраняется в `output/result.json` и содержит:

**Визуальные данные:**
* `country` — страна или территория выпуска
* `postal_type` — тип почты (обычная, авиапочта и т.п.)
* `denomination` — номинал и валюта
* `year_or_period` — год или период выпуска
* `subject` — сюжет или тематика изображения
* `visible_text` — весь читаемый текст на марке
* `colors` — основные доминирующие цвета
* `condition_notes` — визуальные замечания о состоянии
* `uncertainties` — список пунктов с сомнениями
* `confidence` — оценка уверенности распознавания (0-1)

**Справочная информация (`reference_info`):**
* `description` — краткое описание марки и события
* `historical_context` — исторический или тематический контекст
* `purpose` — назначение выпуска (памятный, спортивный и т.п.)
* `info_source` — источник информации (всегда "open sources")
* `verification_note` — пометка о необходимости сверки с каталогами

---

## 🎙 Голосовое описание (JSON -> TTS)

Проект поддерживает автоматическую генерацию голосового описания марки на основе результатов анализа.

### Автоматическая генерация

При запуске анализа с флагом `--tts` автоматически создаются два файла:

```bash
python analyze_stamp.py input/stamp1.jpg --tts
```

После успешного анализа будут созданы:
* `output/voice_script.txt` — текст для озвучки (связное описание марки)
* `output/result.mp3` — аудиофайл с голосовым описанием

### Ручной запуск

Можно также сгенерировать голосовое описание из уже существующего JSON-файла:

```bash
python json_to_voice.py output/result.json
```

### Особенности голосового описания

* **Стиль**: дружелюбный, спокойный, как голосовой помощник филателиста
* **Длительность**: 20-40 секунд (4-7 коротких фраз)
* **Содержание**: связный текст, не сухой перечень полей
* **Ограничения**: упоминание uncertainties и пометка о необходимости сверки с каталогом
* **Запрет**: не выдумываются каталожные номера, тиражи, цены, редкость

---

## 🔧 Настройка

### Переменные окружения

Все секреты хранятся в файле `.env`:

| Переменная       | Описание                    | По умолчанию | Где получить                                                    |
| ---------------- | --------------------------- | ------------ | --------------------------------------------------------------- |
| OPENAI_API_KEY  | API ключ OpenAI             | -            | [OpenAI Platform](https://platform.openai.com/account/api-keys) |
| OPENAI_MODEL    | Модель OpenAI для анализа   | -            | [OpenAI Models](https://platform.openai.com/docs/models) |
| OPENAI_TTS_MODEL| Модель TTS для озвучки      | `tts-1`      | [OpenAI TTS](https://platform.openai.com/docs/guides/text-to-speech) |
| OPENAI_TTS_VOICE| Голос для озвучки           | `alloy`      | `alloy`, `echo`, `fable`, `onyx`, `nova`, `shimmer` |
| OPENAI_TTS_FORMAT| Формат аудиофайла           | `mp3`        | `mp3`, `opus`, `aac`, `flac` |

### Пример `.env` файла:

```env
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1
OPENAI_TTS_MODEL=tts-1
OPENAI_TTS_VOICE=alloy
OPENAI_TTS_FORMAT=mp3
```

---

## 🏗 Архитектура

### Как это работает

1. Скрипт получает путь к изображению из аргументов командной строки
2. Изображение кодируется в base64
3. Формируется запрос к OpenAI API с системным промптом
4. Модель анализирует изображение и извлекает визуальную информацию
5. Модель дополняет результат справочной информацией
6. Результат парсится как JSON и валидируется
7. Результат выводится в консоль и сохраняется в `output/result.json`
8. (Опционально) Если указан флаг `--tts`, генерируется голосовое описание:
   - Создаётся связный текст через OpenAI text model
   - Текст озвучивается через OpenAI TTS API
   - Сохраняются `voice_script.txt` и `result.mp3` в `output/`

### Особенности реализации

* **Детерминированные результаты** — `temperature=0.1` для стабильности
* **JSON-формат ответа** — явное указание `response_format={"type": "json_object"}`
* **Валидация ответа** — проверка наличия данных и корректности JSON
* **Разделение данных** — чёткое разделение визуальных данных и справочной информации
* **Обработка ошибок** — детальная обработка всех возможных ошибок

---

## 🚨 Устранение неполадок

### Ошибка "OPENAI_API_KEY не найден в переменных окружения"

* Убедитесь, что файл `.env` создан в корне проекта
* Проверьте правильность написания переменной `OPENAI_API_KEY`
* Убедитесь, что нет лишних пробелов или кавычек

### Ошибка "OPENAI_MODEL не найден в переменных окружения"

* Проверьте наличие переменной `OPENAI_MODEL` в `.env`
* Убедитесь, что модель поддерживает vision (например, `gpt-4o`, `gpt-4-turbo`)

### Ошибка "401 - Invalid API key"

* Проверьте, что API ключ действителен и не истек
* Убедитесь, что ключ имеет доступ к нужной модели
* Проверьте баланс аккаунта OpenAI

### Ошибка "Файл изображения не найден"

* Убедитесь, что путь к изображению указан правильно
* Проверьте, что файл существует и доступен для чтения
* Используйте относительный путь от корня проекта или абсолютный путь

### Ошибка "Модель вернула пустой ответ"

* Проверьте, что модель поддерживает vision
* Убедитесь, что изображение корректно закодировано
* Попробуйте другое изображение

### Ошибка "Не удалось распарсить JSON ответ от модели"

* Проверьте, что модель поддерживает `response_format={"type": "json_object"}`
* Убедитесь, что системный промпт требует JSON-формат
* Проверьте логи для деталей ошибки

---

## 📝 Лицензия

Этот проект создан в образовательных целях. Используйте ответственно и в соответствии с условиями использования OpenAI API.

---

## 🤝 Вклад в проект

Приветствуются предложения по улучшению:

* Добавление поддержки пакетной обработки изображений
* Улучшение обработки ошибок
* Оптимизация производительности
* Добавление поддержки других форматов вывода
* Улучшение документации

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность API ключа и модели
4. Создайте issue в репозитории

---

## 🎯 Цель проекта

Создать **простой и удобный инструмент** для анализа почтовых марок с использованием AI.  
Каждое изображение — возможность получить детальный анализ и справочную информацию о марке.

---

## 💼 Как проект помогает решать профессиональную задачу

### 📮 **Профессиональная задача:**

Быстрый анализ почтовых марок с извлечением визуальной информации (страна, номинал, год, сюжет) и получением справочных данных о марке и историческом контексте.

### ⚡ **Как проект решает задачу:**

1. **Автоматизация** — анализ изображения без ручного ввода данных
2. **Точность** — использование AI для распознавания текста и элементов
3. **Контекст** — дополнение визуальных данных справочной информацией
4. **Удобство** — простой интерфейс командной строки

### 🔗 **Преимущества:**

* Быстрый анализ изображений марок
* Извлечение всей визуально доступной информации
* Справочная информация о марке и событии
* Сохранение результатов в структурированном JSON-формате
* Чёткое разделение визуальных данных и справочной информации

**Проект делает профессиональный филателистический анализ доступным через простой скрипт.** 🏆

---

_Создано с ❤️ для автоматизации анализа почтовых марок_
